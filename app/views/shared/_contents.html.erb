<div class="container mt-5">
    <div class="row g-4">
        <% @subjects.each do |subject| %>
                <div class="col-lg-4 col-md-6" >
                    <div class="service card-effect">
                        <p><%= subject.subject %></p>
                        <p>投稿者：<%= subject.title %></p>
                        <p><%= subject.name %></p>
                        <p><%= subject.content %></p>
                    </div>
                </div>
        <% end %>
    </div>
</div>