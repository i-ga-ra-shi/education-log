<%# ヘッダー %>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary px-5 py-3 sticky-top">
  <div class="container-fluid">
    <%= link_to("HOME", root_path, class: "navbar-brand fs-2") %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item"><%= link_to("新規投稿", new_subject_path, class: "nav-link fs-5 me-2") %></li>
          <li class="nav-item"><%= link_to("ログアウト", destroy_user_session_path, method: :delete, class: "nav-link fs-5 me-2") %></li>
          <li class="nav-item"><%= link_to("アカウント編集", edit_user_registration_path, class: "nav-link fs-5") %></li>
        <% else %>
          <li class="nav-item">
            <%= link_to("ログイン", new_user_session_path, class: "nav-link fs-5") %>
          </li>
          <li class="nav-item">
            <%= link_to("新規登録", new_user_registration_path, class: "nav-link fs-5") %>
          </li>
        <% end %>
      </ul>
      <form class="d-flex">
        <input class="form-control me-2 bg-light me-4" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-light" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>
<%# ヘッダー %>